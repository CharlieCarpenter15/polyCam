# Core web backend
Flask==3.0.3
Werkzeug==3.0.3

# Configuration
PyYAML==6.0.2
python-dotenv==1.0.1

# HTTP (calendar fetch, Chrome DevTools Protocol discovery, health checks)
requests==2.32.3

# Calendar parsing
icalendar==6.0.1
recurring-ical-events==3.3.4
python-dateutil==2.9.0.post0
tzdata==2024.2

# Chromium control (Chrome DevTools Protocol over websocket)
websocket-client==1.8.0
