# Optional: Poly remote / HID controller support.
# Needs apt packages: python3-dev, build-essential (installed by scripts/install.sh).
# The appliance runs fine without this: remote_service degrades to a no-op.
evdev==1.7.1

# The meeting-minutes feature has its own extras in requirements-minutes.txt.
# They are deliberately NOT here: this file is installed on every appliance,
# and those are hundreds of megabytes of machine-learning wheels that most
# rooms will never switch on. Install them with:
#
#     scripts/install-minutes.sh
#
# or by hand into the appliance's virtualenv.
